import 'package:flutter/material.dart';

/// Global admin announcements reading page (Lot D completes UI and behavior).
class GlobalAnnouncementsPage extends StatelessWidget {
  const GlobalAnnouncementsPage({super.key});

  static const String routePath = '/announcements';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SizedBox.shrink(),
    );
  }
}
