import 'package:flutter/material.dart';

/// Fixed colors that are not tied to any palette or theme variant.
abstract final class StaticColors {
  static const Color background = Color(0xFFF5F4FC);

  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color cardBorder = Color(0xFFECEBFA);
  // rgba(63, 70, 247, 0.08)
  static const Color cardShadowColor = Color(0x143F46F7);

  static const Color lightBackground = Color(0xFFEEF0FF);
}
