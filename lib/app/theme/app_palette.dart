import 'package:flutter/material.dart';

/// Centralized app colors for consistent branding across platforms.
class AppPalette {
  const AppPalette._();

  // Brand palette (from approved Figma set).
  static const Color primary = Color(0xFF97264E);
  static const Color primaryLight = Color(0xFFE798DC);
  static const Color primarySoft = Color(0xFFF3CDEE);
  static const Color accent = Color(0xFFCF30B8);
  static const Color secondary = Color(0xFF7ECFDD);
  static const Color success = Color(0xFF4DC75E);
  static const Color warning = Color(0xFFAE8F56);
  static const Color deep = Color(0xFF2E0B29);

  // App semantic colors.
  static const Color successContainer = Color(0xFFE8F8EA);
}
